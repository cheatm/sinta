version: '3'
services:
  sinta:
    container_name: sinta
    build: .
    image: sinta
    volumes:
      - ".:/data"
      - ".:/logs"
#    environment:
#      PRE_CHECK:
#      CREATE_OPTION: "-s 2017-05-10"
#      SYNC_TABLE: "update"
